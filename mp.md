# 商管营销宝 - 开发任务拆解清单

> 文档版本：V1.0 | 基于需求规格说明书 V1.1 拆解
> 使用说明：每个功能点前有 `- [ ]` 复选框，完成后可勾选标记进度

---

## 一、公共基础模块（优先开发）

### 1.1 用户认证与登录
- [ ] **登录页（前端）**
  - 账号/密码输入框
  - "记住账号"复选框（本地存储）
  - 登录按钮 + 加载状态
  - "忘记密码"跳转链接
- [ ] **登录接口（后端）**
  - `POST /api/auth/login`
  - 校验账号密码 → 返回用户信息 + Token
  - 失败返回错误提示（账号不存在/密码错误/账号禁用）
- [ ] **重置密码页面（前端）**
  - 手机号/邮箱验证（按实际实现）
  - 新密码设置 + 确认
- [ ] **重置密码接口（后端）**
  - `POST /api/auth/reset-password`

---

### 1.1.1 系统管理员与普通管理员权限区分（0907新增）
- [x] **后端：User 模型新增 `isSystemAdmin` 字段**
  - 系统管理员（唯一账号）拥有全部权限
  - 新增管理员（多项目）仅可调阅、查阅所有信息，不开放新增项目/角色/公司
- [x] **后端：新增 `requireSystemAdmin` 中间件**
  - 应用于项目/用户/公司的 POST/PUT 路由
- [x] **后端：`serializeUser` 暴露 `isSystemAdmin` 给前端**
- [x] **前端：管理员工作台隐藏系统管理员专属入口**
  - 用户管理、合作公司管理、项目配置入口仅系统管理员可见
- [x] **前端：用户/公司/项目页面隐藏新建/编辑按钮**
  - 非系统管理员仅可查看，不显示新建、编辑、重置密码按钮

---

### 1.2 多项目选择与切换
- [ ] **项目选择弹窗（前端）**
  - 登录成功后强制弹出
  - 展示当前账号有权限的项目列表（支持多选/单选）
  - 确定后进入工作台
- [ ] **获取用户项目列表接口（后端）**
  - `GET /api/user/projects`
  - 返回：`[{ projectId, projectName, isDefault }]`
- [ ] **项目切换（前端）**
  - 个人中心内提供"切换项目"入口
  - 切换后刷新全局数据（所有列表/统计重新请求）
- [ ] **项目切换接口（后端）**
  - `POST /api/user/switch-project`
  - 更新当前会话项目上下文

---

### 1.3 数据隔离与权限控制（后端架构）
- [ ] **权限拦截器**
  - 所有接口校验用户角色（销售员/渠道员/管理员）
  - 越权访问返回 403
- [ ] **数据隔离中间件**
  - 销售员/渠道员：只能查询/操作自己归属 + 当前选中项目的数据
  - 管理员：可跨项目查看，需注入项目列表过滤
- [ ] **角色枚举定义**
  - `ROLE_SALES`、`ROLE_CHANNEL`、`ROLE_ADMIN`

### 1.4 文件系统 - 阿里云 OSS（0907新增）
- [x] **后端：新增 OSS 配置（env 驱动）**
  - `OSS_ACCESS_KEY_ID` / `OSS_ACCESS_KEY_SECRET` / `OSS_REGION` / `OSS_BUCKET` / `OSS_ENDPOINT`
  - 未配置时自动回退本地磁盘存储
- [x] **后端：水印照片上传支持 OSS**
  - `saveWatermarkedPhoto` 优先上传 OSS，失败回退本地
- [x] **后端：通用文件上传支持 OSS**
  - `uploadController.generic` 改为 memoryStorage，优先 OSS
- [x] **后端：`.env.example` 补充 OSS 配置示例**
  - AccessKey ID / AccessKey Secret 由用户后续自行配置
---

## 二、销售员模块

### 2.1 工作台首页（销售员）
- [ ] **统计卡片（前端）**
  - 我的客户总数
  - 待跟进客户
  - 30天未跟进预警（红色高亮数字）
  - 已成交客户
  - 未到访申领池(数量)
- [ ] **统计接口（后端）**
  - `GET /api/sales/dashboard`
  - 入参：`projectId`
  - 返回统计数字
- [ ] **快捷按钮（前端）**
  - 新增客户录入
  - 我的客户列表
  - 公共池客户
  - 未到访申领池
  - 跟进提醒

---

### 2.2 新增客户录入（销售员/管理员/渠道员）
> 0907新增：销售员、管理员、渠道员三种角色的录入表单区分开

#### 2.2.1 销售员录入表单（前端）
- [x] **到访渠道限制为 自访/自拓**
- [x] **到访时间字段（日期+时间选择器，必填）**
- [x] **其余字段**：姓名、电话、年龄、意向评级、水印照片、备注

#### 2.2.2 管理员录入表单（前端 + 后端）
- [x] **到访渠道可选全部**：自访、自拓、渠道公司推荐、个人推荐
- [x] **选择"渠道公司推荐"时下拉选择合作公司**
- [x] **选择"个人推荐"时填写推荐人姓名**
- [x] **到访时间改为"预计到访时间"**
- [x] **可选指定归属销售员**
- [x] **后端：`POST /api/admin/customer` 新增客户接口**
  - 支持全部到访渠道 + 渠道公司关联 + 可选归属人

#### 2.2.3 渠道员推荐录入（前端 + 后端）
> 0907新增：渠道员只负责推荐，不录入完整客户档案
- [x] **表单简化为**：选择渠道公司（第一行）→ 姓名 → 电话 → 预计到访时间 → 备注
- [x] **录入页面第一行就是选择渠道公司**
- [x] **推荐信息仅保留**：姓名、电话、推荐公司、预计到访时间
- [x] **手机号前三后四脱敏提交**
- [x] **24小时保护期**：过期需重新报备
- [x] **客户到了之后**：销售员申领客户 → 点击到访

#### 2.2.4 手机号加密逻辑（0907新增）
- [x] **非管理员只能看自己客户的手机号**
- [x] **公共池列表**：非归属人手机号脱敏
- [x] **未到访申领池**：客户归属未定，一律脱敏
- [x] **销售员客户详情**：查看公共池客户时手机号脱敏
- [x] **管理员**：可查看全部手机号

#### 2.2.5 原有公共逻辑
- [ ] **提交按钮逻辑（前端）**
  - 前端校验所有必填项
  - 提交前防抖查询手机号是否存在
- [ ] **手机号查重接口（后端）**
  - `POST /api/sales/customer/check-duplicate`
  - 扫描：我的客户 + 公共池 + 渠道推荐 + 过期池（同一项目）
  - 返回：是否存在 + 冲突详情
- [ ] **客户提交接口（后端）**
  - 正常保存：`POST /api/sales/customer`
  - 时间校验：到访时间不能晚于系统时间4小时以上
  - 图片自动加水印（项目名+时间）
- [ ] **冲突审批发起（后端）**
  - 查重冲突时自动生成审批单
  - `POST /api/approval/submit`（type = CUSTOMER_CONFLICT）
- [ ] **审批通过回调逻辑（后端）**
  - 创建客户档案，归属当前销售员 + 当前项目
---

### 2.3 我的客户列表（销售员）
- [ ] **列表页（前端）**
  - 展示字段：姓名、电话、意向等级、到访时间、最近跟进时间、客户来源、客户状态
  - 筛选条件：意向等级、到访渠道、跟进时间范围、是否到访
  - 搜索：姓名、手机号
  - 点击列表项 → 跳转客户详情
- [ ] **列表接口（后端）**
  - `GET /api/sales/customers`
  - 入参：分页 + 筛选条件 + 搜索关键字
  - 返回客户列表（仅归属自己的）

---

### 2.4 销售员-客户详情页
- [ ] **基础信息展示（前端）**
  - 全部基础信息 + 水印照片展示（支持预览）
- [ ] **意向评级修改（前端）**
  - 下拉切换，修改后立即保存
- [ ] **修改意向接口（后端）**
  - `PUT /api/sales/customer/{id}/intent`
  - 记录操作日志
- [ ] **编辑客户基础信息（前端）**
  - 仅归属本人可编辑
- [ ] **编辑客户接口（后端）**
  - `PUT /api/sales/customer/{id}`
  - 记录修改前后值日志
- [ ] **跟进记录列表（前端）**
  - 按时间倒序展示全部历史跟进记录
- [ ] **跟进记录接口（后端）**
  - `GET /api/sales/followups/{customerId}`
- [ ] **新增跟进记录（前端）**
  - 表单：跟进时间、跟进方式（电话/微信/面谈）、跟进内容、跟进结果
- [ ] **新增跟进接口（后端）**
  - `POST /api/sales/followup`
  - 更新客户的最近跟进时间

---

### 2.5 公共池客户（销售员）
- [ ] **列表页（前端）**
  - 展示字段：姓名、手机号、来源、意向等级、移入公共池时间
  - 筛选 + 搜索
  - 每条记录操作按钮【申请认领】
- [ ] **列表接口（后端）**
  - `GET /api/sales/public-pool`
- [ ] **申请认领接口（后端）**
  - `POST /api/approval/claim-public`
  - 生成审批待办给管理员

---

### 2.6 未到访申领池（销售员）
- [ ] **列表页（前端）**
  - 展示字段：姓名、手机号、推荐类型、渠道推荐人、推荐时间、报备时长
  - 筛选：推荐类型、报备时长
  - 操作按钮【申领该客户】
  - 0907新增：手机号一律脱敏（客户归属未定）
- [ ] **列表接口（后端）**
  - `GET /api/sales/unvisited-pool`
- [ ] **申领接口（后端）**
  - `POST /api/approval/claim-unvisited`
  - 生成审批待办给管理员

---

### 2.7 跟进提醒（销售员）
- [ ] **提醒列表（前端）**
  - 展示即将满30天未跟进的客户（红色预警标签）
  - 点击跳转客户详情页
- [ ] **提醒接口（后端）**
  - `GET /api/sales/followup-reminder`

---

### 2.8 销售员-个人中心
- [ ] 展示当前授权项目列表 + 切换
- [ ] 修改密码
- [ ] 退出登录

---

## 三、渠道员模块

### 3.1 工作台首页（渠道员）
- [ ] **统计卡片（前端）**
  - 我的推荐客户总数
  - 已到访客户
  - 未到访客户
  - 过期客户池(数量)
- [ ] **统计接口（后端）**
  - `GET /api/channel/dashboard`
- [ ] **快捷按钮（前端）**
  - 新增推荐客户录入
  - 我的推荐客户列表
  - 过期客户池
  - 个人中心

---

### 3.2 新增推荐客户录入（渠道员）
> 0907新增：渠道员只负责推荐，表单简化，移除 A/B 类型切换
- [x] **表单页面（前端）**
  - 第一行：选择渠道公司（下拉，读取系统配置）
  - 客户姓名（必填）
  - 电话（必填，提交时前三后四脱敏）
  - 预计到访时间（日期+时间选择器，必填）
  - 备注（多行文本）
- [x] **保存接口（后端）**
  - `POST /api/channel/customer`
  - 时间校验：推荐时间不能早于录入时间4小时以上
  - 保存后开始24小时倒计时
  - 生成操作日志

---

### 3.3 我的推荐客户列表（渠道员）
- [ ] **列表页（前端）**
  - 展示字段：姓名、手机号（脱敏）、推荐公司、推荐时间、是否已到访、到访确认倒计时
  - 筛选：是否到访、时间范围
  - 搜索：姓名
  - 点击跳转渠道客户详情页
- [ ] **列表接口（后端）**
  - `GET /api/channel/customers`

---

### 3.4 渠道客户详情页
- [ ] **未到访未过期状态（前端）**
  - 渠道员可编辑客户信息
  - 可标记【已到访】
- [ ] **已到访状态（前端）**
  - 只读模式
  - 可查看销售员跟进记录
- [ ] **已过期状态（前端）**
  - 页面提示"客户已过期，请前往过期客户池重新报备"
- [ ] **编辑接口（后端）**
  - `PUT /api/channel/customer/{id}`（仅未到访未过期可操作）
  - 记录编辑日志
- [ ] **标记已到访接口（后端）**
  - `POST /api/channel/customer/{id}/arrive`
  - 关联接待销售员（需指定）
  - 状态切换为只读

---

### 3.5 过期客户池（渠道员）
- [ ] **列表页（前端）**
  - 展示字段：姓名、推荐类型、原合作公司/原经纪人、原推荐时间、过期时间
  - 操作按钮【重新报备】
- [ ] **列表接口（后端）**
  - `GET /api/channel/expired-pool`
- [ ] **重新报备接口（后端）**
  - `POST /api/channel/report-expired`
  - 生成全新报备记录，重新开启24h倒计时
  - 旧记录归档保留，不可删除

---

### 3.6 渠道员-个人中心
- [ ] 项目切换
- [ ] 修改密码
- [ ] 退出登录

---

## 四、管理员模块

### 4.1 管理工作台首页
- [ ] **统计总览（前端）**
  - 全部客户总数
  - 销售员录入客户
  - 渠道推荐客户
  - 公共池客户
  - 过期池客户
  - 待审批申领数量（高亮）
- [ ] **统计接口（后端）**
  - `GET /api/admin/dashboard`（支持多项目汇总）
- [ ] **快捷按钮（前端）**
  - 新增客户录入（0907新增）
  - 全部客户管理
  - 公共池客户管理
  - 未到访申领审批
  - 过期客户池管理
  - 客户数据导出
  - 用户管理（仅系统管理员可见）
  - 合作公司管理（仅系统管理员可见）
  - 项目配置（仅系统管理员可见）
  - 系统参数
  - 操作日志

---

### 4.2 全部客户管理（管理员）
- [ ] **列表页（前端）**
  - 展示字段：姓名、手机号、来源类型、归属人、所属项目、意向等级、最近跟进时间、客户状态
  - 多维筛选：项目、来源类型、归属人、意向等级、客户状态、时间范围
  - 搜索：姓名、手机号
  - 点击进入管理员客户详情
- [ ] **列表接口（后端）**
  - `GET /api/admin/customers`

---

### 4.3 管理员-客户详情页
- [ ] **完整信息展示（前端）**
  - 全部基础信息 + 水印照片
- [ ] **编辑客户信息（前端）**
  - 管理员可修改任意字段
- [ ] **编辑接口（后端）**
  - `PUT /api/admin/customer/{id}`
  - 强制记录：修改人、时间、修改前值、修改后值
- [ ] **跟进记录完整查看（前端）**
- [ ] **跟进记录接口（后端）**
  - `GET /api/admin/followups/{customerId}`
- [ ] **客户归属调整（前端）**
  - 分配给销售员/渠道员
  - 移入公共池
- [ ] **归属调整接口（后端）**
  - `POST /api/admin/assign-customer`
  - 操作写日志
- [ ] **完整流转日志（前端）**
  - 展示报备、过期、申领、分配、审批全链路
- [ ] **流转日志接口（后端）**
  - `GET /api/admin/customer/{id}/transfer-log`

---

### 4.4 公共池客户管理（管理员）
- [ ] **列表页（前端）**
  - 展示公共池全部客户
- [ ] **认领审批列表（前端）**
  - 销售员提交的认领申请
  - 操作：【同意】【驳回】
- [ ] **认领审批接口（后端）**
  - `POST /api/admin/approve-claim`
  - 审批写日志
  - 同意：客户归属销售员；驳回：退回公共池
- [ ] **手动分配接口（后端）**
  - `POST /api/admin/assign-from-public`
  - 直接分配给销售员/渠道员

---

### 4.5 未到访申领审批（管理员）
- [ ] **待办列表（前端）**
  - 展示：申领销售员、客户信息、原渠道推荐人、申请时间
  - 操作：【审批同意】【审批驳回】
- [ ] **待办列表接口（后端）**
  - `GET /api/admin/unvisited-approvals`
- [ ] **审批接口（后端）**
  - `POST /api/admin/approve-unvisited`
  - 同意：客户归属申领销售员；驳回：退回未到访申领池
  - 全部动作记录日志

---

### 4.6 过期客户池管理（管理员）
- [ ] **列表页（前端）**
  - 查看所选项目全部渠道过期报备客户
  - 查看原始报备记录
  - 查看是否做过重新报备
- [ ] **列表接口（后端）**
  - `GET /api/admin/expired-pool`
  - 注：管理员仅查看，不能编辑

---

### 4.7 客户数据导出（管理员）
- [ ] **导出筛选表单（前端）**
  - 所属项目、时间范围、客户来源、归属人、意向等级、客户状态
- [ ] **导出按钮 + 文件下载（前端）**
  - 点击后下载 Excel 文件
- [ ] **导出接口（后端）**
  - `POST /api/admin/export-customers`
  - 返回文件流（Excel）
  - 强制记录导出日志：导出账号、导出时间、筛选条件
  - 支持大数据量异步导出（可选）

---

### 4.8 系统配置（管理员）
> 0907新增：项目管理、账号角色管理、合作公司管理仅系统管理员可操作
- [ ] **项目管理（前端）**
  - 新增、编辑、停用项目
  - 仅系统管理员可见新建/编辑按钮
- [ ] **项目管理接口（后端）**
  - `GET/POST/PUT /api/admin/projects`
  - POST/PUT 受 `requireSystemAdmin` 保护
- [ ] **账号角色管理（前端）**
  - 新增/禁用/重置密码 销售员、渠道员、管理员账号
  - 分配可访问项目
  - 仅系统管理员可见新建/编辑/重置密码按钮
- [ ] **账号管理接口（后端）**
  - `GET/POST/PUT /api/admin/users`
  - `POST /api/admin/users/{id}/reset-password`
  - POST/PUT/重置密码受 `requireSystemAdmin` 保护
- [ ] **合作公司管理（前端）**
  - 维护渠道客户下拉选项
  - 仅系统管理员可见新建/编辑按钮
- [ ] **合作公司接口（后端）**
  - `GET/POST/PUT /api/admin/companies`
  - POST/PUT 受 `requireSystemAdmin` 保护
- [ ] **系统参数配置（前端）**
  - 客户无跟进自动进公共池天数（默认30）
  - 渠道报备自动过期小时（默认24）
  - 销售员到访时间最大偏移小时（默认4）
  - 渠道推荐时间最大偏移小时（默认4）
- [ ] **系统参数接口（后端）**
  - `GET/PUT /api/admin/config`

---

### 4.9 操作日志（管理员）
- [ ] **日志列表（前端）**
  - 不可删除
  - 日志类型：客户信息修改、客户归属变更、移入公共池、认领审批、未到访申领审批、渠道过期、重新报备、数据导出
  - 筛选：项目、操作人员、时间范围
- [ ] **日志列表接口（后端）**
  - `GET /api/admin/audit-logs`

---

### 4.10 管理员-个人中心
- [ ] 项目切换
- [ ] 修改密码
- [ ] 退出登录

---

## 五、定时任务与后端服务（独立部署）

- [ ] **渠道24小时过期检查任务**
  - 扫描渠道报备表中未到访且推荐时间 < NOW() - 24h 的记录
  - 移入过期客户池
  - 生成日志
- [ ] **客户30天无跟进移入公共池任务**
  - 扫描客户表最近跟进时间 < NOW() - 30d
  - 移入公共池
  - 生成日志
- [ ] **图片水印服务**
  - 接收上传图片 + 项目名称 + 时间
  - 返回水印后图片URL

---

## 六、全局公共校验规则（后端强制）

- [ ] 手机号查重规则（A类脱敏除外）
- [ ] 销售员到访时间4小时偏移校验
- [ ] 渠道推荐时间4小时偏移校验
- [ ] 所有修改/分配/审批/导出操作写日志
- [ ] 日志禁止物理删除
- [ ] 权限隔离（销售员/渠道员/管理员数据可见范围）

---

## 七、非功能需求

- [ ] 小程序端数据按项目严格隔离
- [ ] 图片上传自动水印（项目名+系统时间）
- [ ] 时间校验逻辑后端为主、前端为辅
- [ ] Excel导出支持大数据量（可异步）
- [ ] 所有审批操作必须记录日志，日志不可删除

---

## 八、接口开发顺序（建议Sprint拆分）

| 优先级 | 模块 | 接口数量（预估） |
| :---: | :--- | :---: |
| P0 | 登录 + 权限 + 项目切换 + 数据隔离 | 5 |
| P1 | 销售员-新增客户 + 列表 + 详情 + 跟进 | 8 |
| P2 | 渠道员-报备 + 列表 + 详情 + 过期重报 | 6 |
| P3 | 公共池 + 未到访申领 + 全部审批流程 | 10 |
| P4 | 管理员-全量管理 + 分配 + 导出 + 配置 | 15 |
| P5 | 定时任务 + 日志审计 + 非功能完善 | 4 |

---

> **文档结束**
> 下一步建议：复制本文件到项目管理工具（如TAPD、Jira、飞书文档），按模块指派给对应开发人员，逐项拆解为开发任务。


